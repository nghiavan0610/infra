# =============================================================================
# GitHub Actions Self-Hosted Runner Configuration
# =============================================================================

# Runner name (shows in GitHub UI)
GITHUB_RUNNER_NAME=my-vps-runner

# Registration token (REQUIRED)
# Get from: GitHub repo → Settings → Actions → Runners → New self-hosted runner
# Token starts with: AAAAA...
GITHUB_RUNNER_TOKEN=

# Runner scope: repo, org, or enterprise
GITHUB_RUNNER_SCOPE=repo

# For repo-level runner (REQUIRED if scope=repo)
# Example: https://github.com/username/repo
GITHUB_REPO_URL=

# For org-level runner (use instead of REPO_URL if scope=org)
# GITHUB_ORG_NAME=my-organization

# Runner labels (comma-separated, used in runs-on)
# Default labels: self-hosted, linux, x64 (added automatically)
GITHUB_RUNNER_LABELS=docker,production

# Runner group (for org runners only)
GITHUB_RUNNER_GROUP=default

# Ephemeral mode (runner exits after each job - more secure)
# Set to true for better security, false for persistent runner
GITHUB_RUNNER_EPHEMERAL=false

# Resource limits
GITHUB_RUNNER_CPU_LIMIT=2
GITHUB_RUNNER_MEMORY_LIMIT=4G
