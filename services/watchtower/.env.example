# =============================================================================
# Watchtower Configuration
# =============================================================================

# Update schedule (cron format: second minute hour day month weekday)
# Default: 4 AM daily
WATCHTOWER_SCHEDULE=0 0 4 * * *

# Include stopped containers in updates
WATCHTOWER_INCLUDE_STOPPED=false

# Restart stopped containers after update
WATCHTOWER_REVIVE_STOPPED=false

# Rolling restart (update one container at a time)
WATCHTOWER_ROLLING_RESTART=false

# Only update containers with watchtower label
# Set to true for more control over which containers get updated
WATCHTOWER_LABEL_ENABLE=false

# Debug logging
WATCHTOWER_DEBUG=false

# =============================================================================
# Notifications (optional)
# =============================================================================
# Notification type: email, slack, msteams, gotify, shoutrrr
# WATCHTOWER_NOTIFICATIONS=shoutrrr

# Shoutrrr URL for notifications (supports many services)
# Slack: slack://token-a/token-b/token-c
# Discord: discord://token@id
# Ntfy: ntfy://ntfy.example.com/topic
# Email: smtp://user:pass@host:port/?from=x&to=y
# WATCHTOWER_NOTIFICATION_URL=
