# =============================================================================
# Vaultwarden Configuration
# =============================================================================

# Domain (required - your Vaultwarden URL)
# Used by clients to connect
VAULTWARDEN_DOMAIN=http://localhost:8222

# Port configuration
VAULTWARDEN_PORT=8222
VAULTWARDEN_WS_PORT=3012

# Admin token (required - generate with: openssl rand -base64 48)
# Access admin panel at /admin
VAULTWARDEN_ADMIN_TOKEN=

# Signup settings
SIGNUPS_ALLOWED=true           # Allow new user registration
SIGNUPS_VERIFY=false           # Require email verification
INVITATIONS_ALLOWED=true       # Allow user invitations

# Log level (trace, debug, info, warn, error)
VAULTWARDEN_LOG_LEVEL=info

# =============================================================================
# Email Configuration (optional - for password recovery, invitations)
# =============================================================================
# VAULTWARDEN_SMTP_HOST=smtp.gmail.com
# VAULTWARDEN_SMTP_PORT=587
# VAULTWARDEN_SMTP_FROM=vaultwarden@example.com
# VAULTWARDEN_SMTP_SECURITY=starttls
# VAULTWARDEN_SMTP_USERNAME=
# VAULTWARDEN_SMTP_PASSWORD=

# =============================================================================
# Traefik Configuration (if exposing publicly)
# =============================================================================
# VAULTWARDEN_HOST=vault.example.com
