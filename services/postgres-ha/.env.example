# =============================================================================
# PostgreSQL Primary-Replica Configuration
# =============================================================================
# Copy this file to .env and update the values
# DO NOT commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Admin Credentials (Superuser)
# -----------------------------------------------------------------------------
# Used for administrative tasks and replication setup
# Generate with: openssl rand -base64 32
POSTGRES_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# -----------------------------------------------------------------------------
# Replication User Credentials
# -----------------------------------------------------------------------------
# Used for streaming replication between master and replica
# Generate with: openssl rand -base64 32
POSTGRES_REPLICATION_USERNAME=repuser
POSTGRES_REPLICATION_PASSWORD=CHANGE_ME_REPLICATION_PASSWORD

# -----------------------------------------------------------------------------
# Application User Credentials
# -----------------------------------------------------------------------------
# Used by your application to connect to the database
# Generate with: openssl rand -base64 32
POSTGRES_USERNAME=app_user
POSTGRES_PASSWORD=CHANGE_ME_APP_PASSWORD

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Default database created on startup
POSTGRESQL_DATABASE=myapp

# Application schema (for Prisma, etc.)
POSTGRES_APP_SCHEMA=public

# -----------------------------------------------------------------------------
# Port Configuration
# -----------------------------------------------------------------------------
# Master port (read-write operations)
POSTGRES_MASTER_PORT=5432
# Replica port (read-only operations)
POSTGRES_REPLICA_PORT=5433

# -----------------------------------------------------------------------------
# Backup Configuration
# -----------------------------------------------------------------------------
# Backups are handled by centralized backup in ../backup/
# See ../backup/README.md for documentation

# =============================================================================
# Security Notes:
# =============================================================================
# 1. Use strong passwords (32+ characters recommended)
# 2. Never commit this file with real credentials
# 3. Use different passwords for each user
# 4. Rotate credentials periodically
# =============================================================================
