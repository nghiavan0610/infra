# =============================================================================
# Plausible Analytics Configuration
# =============================================================================

# Base URL (required - your public Plausible URL)
PLAUSIBLE_BASE_URL=http://localhost:8000

# Secret keys (required - generate with: openssl rand -base64 48)
PLAUSIBLE_SECRET_KEY=
PLAUSIBLE_TOTP_KEY=

# Database configuration (uses dedicated postgres)
PLAUSIBLE_DB_USER=plausible
PLAUSIBLE_DB_PASSWORD=
PLAUSIBLE_DB_NAME=plausible

# Port configuration
PLAUSIBLE_PORT=8000

# Disable registration after creating admin account
PLAUSIBLE_DISABLE_REGISTRATION=false

# Email configuration (optional)
# PLAUSIBLE_MAILER_EMAIL=plausible@example.com
# PLAUSIBLE_SMTP_HOST=mailpit
# PLAUSIBLE_SMTP_PORT=1025

# Domain for Traefik (if exposing publicly)
# PLAUSIBLE_DOMAIN=analytics.example.com
