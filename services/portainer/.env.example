# =============================================================================
# Portainer Configuration
# =============================================================================

# Admin password (set on first login if not specified)
# Generate with: htpasswd -nb admin yourpassword | cut -d: -f2
PORTAINER_ADMIN_PASSWORD=

# Port configuration
PORTAINER_PORT=9444       # HTTPS (changed from 9443 to avoid conflict with Authentik)
PORTAINER_HTTP_PORT=9001  # HTTP (changed from 9000 to avoid conflict with Authentik)

# Domain for Traefik (if exposing publicly)
# PORTAINER_DOMAIN=portainer.example.com
