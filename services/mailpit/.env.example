# =============================================================================
# Mailpit Configuration
# =============================================================================

# SMTP port (your apps send to this)
SMTP_PORT=1025

# Web UI port
MAILPIT_UI_PORT=8025

# Max emails to keep (oldest deleted when exceeded)
MP_MAX_MESSAGES=1000

# =============================================================================
# Authentication (optional)
# =============================================================================
# Web UI auth (format: user:password)
# MP_UI_AUTH=admin:password

# SMTP auth (format: user:password)
# MP_SMTP_AUTH=user:password

# =============================================================================
# Traefik (optional)
# =============================================================================
# MAILPIT_DOMAIN=mail.example.com
