# =============================================================================
# Faster-Whisper Production Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
WHISPER_PORT=8000

# -----------------------------------------------------------------------------
# CPU Model Configuration
# -----------------------------------------------------------------------------
# Models: tiny, base, small, medium, large-v2, large-v3
WHISPER_MODEL=small
WHISPER_COMPUTE_TYPE=int8

# -----------------------------------------------------------------------------
# GPU Model Configuration (used with --profile gpu)
# -----------------------------------------------------------------------------
WHISPER_MODEL_GPU=large-v3
WHISPER_COMPUTE_TYPE_GPU=float16

# -----------------------------------------------------------------------------
# Resource Limits - CPU
# -----------------------------------------------------------------------------
WHISPER_CPU_LIMIT=4
WHISPER_CPU_RESERVATION=1
WHISPER_MEMORY_LIMIT=4G
WHISPER_MEMORY_RESERVATION=1G

# -----------------------------------------------------------------------------
# Resource Limits - GPU
# -----------------------------------------------------------------------------
WHISPER_MEMORY_LIMIT_GPU=12G

# -----------------------------------------------------------------------------
# Traefik Integration
# -----------------------------------------------------------------------------
TRAEFIK_ENABLED=false
WHISPER_DOMAIN=whisper.example.com

# =============================================================================
# Usage:
#   CPU:  docker compose up -d
#   GPU:  docker compose --profile gpu up -d
#
# With Traefik:
#   1. Set TRAEFIK_ENABLED=true
#   2. Set WHISPER_DOMAIN=your-domain.com
#   3. Uncomment traefik-public network in docker-compose.yml
#   4. docker compose up -d
# =============================================================================

# =============================================================================
# Model Reference
# =============================================================================
# | Model    | RAM (CPU) | VRAM (GPU) | Speed   | Accuracy |
# |----------|-----------|------------|---------|----------|
# | tiny     | ~1GB      | ~1GB       | Fastest | Low      |
# | base     | ~1GB      | ~1GB       | Fast    | Fair     |
# | small    | ~2GB      | ~2GB       | Good    | Good     |
# | medium   | ~5GB      | ~5GB       | Slow    | High     |
# | large-v3 | ~10GB     | ~10GB      | Slowest | Best     |
# =============================================================================
