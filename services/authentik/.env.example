# =============================================================================
# Authentik Configuration
# =============================================================================

# Domain (required for production)
AUTHENTIK_DOMAIN=auth.example.com

# Secret key (generate with: openssl rand -base64 60)
AUTHENTIK_SECRET_KEY=change-me-generate-with-openssl-rand-base64-60

# Bootstrap admin password (change after first login)
AUTHENTIK_BOOTSTRAP_PASSWORD=change-me-strong-password

# Bootstrap admin email
AUTHENTIK_BOOTSTRAP_EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# Database (use existing or bundled)
# -----------------------------------------------------------------------------
# Option 1: Use existing PostgreSQL from your infra
# POSTGRES_HOST=host.docker.internal
# POSTGRES_PORT=5432
# POSTGRES_DB=authentik
# POSTGRES_USER=authentik
# POSTGRES_PASSWORD=change-me

# Option 2: Use bundled PostgreSQL (default)
POSTGRES_PASSWORD=change-me-db-password

# -----------------------------------------------------------------------------
# Redis (use existing or bundled)
# -----------------------------------------------------------------------------
# Option 1: Use existing Redis from your infra
# REDIS_HOST=host.docker.internal
# REDIS_PORT=6379

# Option 2: Use bundled Redis (default - no config needed)

# -----------------------------------------------------------------------------
# Ports
# -----------------------------------------------------------------------------
AUTHENTIK_HTTP_PORT=9000
AUTHENTIK_HTTPS_PORT=9443

# -----------------------------------------------------------------------------
# Email (optional - for password reset, notifications)
# -----------------------------------------------------------------------------
# AUTHENTIK_EMAIL__HOST=smtp.example.com
# AUTHENTIK_EMAIL__PORT=587
# AUTHENTIK_EMAIL__USERNAME=user
# AUTHENTIK_EMAIL__PASSWORD=pass
# AUTHENTIK_EMAIL__USE_TLS=true
# AUTHENTIK_EMAIL__FROM=authentik@example.com

# -----------------------------------------------------------------------------
# Resource Limits
# -----------------------------------------------------------------------------
MEMORY_LIMIT_SERVER=1g
MEMORY_LIMIT_WORKER=1g
CPU_LIMIT=2
