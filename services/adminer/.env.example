# =============================================================================
# Adminer Configuration
# =============================================================================

# Port
ADMINER_PORT=8081

# Default database server (shown in login form)
DEFAULT_DB_HOST=host.docker.internal

# Theme (options: pepa-linha-dark, nette, hydra, etc.)
ADMINER_DESIGN=pepa-linha-dark

# Plugins (space-separated)
ADMINER_PLUGINS=tables-filter tinymce

# =============================================================================
# Traefik (optional)
# =============================================================================
# ADMINER_DOMAIN=adminer.example.com
# ADMINER_BASIC_AUTH=admin:$$apr1$$xyz...  # Generate with: htpasswd -n admin
